namespace winui_solution_app_tests;

// A WinExe needs an entry point. Real test projects get one synthesized by the
// test SDK; this sample declares a trivial one so the project builds standalone.
internal static class Program
{
    private static void Main()
    {
    }
}
